import 'package:flutter/material.dart';
class intro_page_three extends StatelessWidget {
  const intro_page_three({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.blue.shade300,
    );
  }
}